// /practice modules that are fully built and verified but held back from
// public view because no teaching guide backs the concept yet - this site's
// practice modules are meant to reinforce something a reader can also go
// learn about (SQL pairs with guides/databases, git pairs with
// guides/version-control, etc.); a module with no conceptual guide behind it
// is orphaned from the site's actual teaching content. Remove a slug here
// once a real guide for that topic exists (and add a relatedGuide mapping
// for it in $lib/practice/related-guides/).
export const HIDDEN_MODULES = new Set(['webassembly']);
