>[Milvus](https://milvus.io/docs/overview.md) is a database that stores, indexes, and manages
> massive embedding vectors generated by deep neural networks and other machine learning (ML) models.

## Installation and setup

Install the Python SDK:

```lc-tabs
[
 {
  "label": "pip",
  "lang": "bash",
  "code": "pip install langchain-milvus"
 },
 {
  "label": "uv",
  "lang": "bash",
  "code": "uv add langchain-milvus"
 }
]
```

## Vector store

See a [usage example](lc:oss/python/integrations/vectorstores/milvus).

To import this vectorstore:
```python
from langchain_milvus import Milvus
```
