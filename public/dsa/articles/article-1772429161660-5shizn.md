# Datatypes in Java

> **Slug:** `article-1772429161660-5shizn`  
> **Published:** 2026-04-16T16:27:49.896Z  
> **Updated:** 2026-04-16T16:27:49.935Z  
> **Keywords:** Datatypes in java  
> **Cover Image:** ![Datatypes in Java](69d100ae547d873896119074)

**Description:** Learn Java data types, primitive vs reference types, ranges, memory sizes, and why choosing the right type matters in coding and interviews.

---

# **Java Data Types**

In the previous tutorial, you learned about variables and literals. Now let’s understand something critical:

> [!NOTE]
> **INFO**
> **What kind of data can a variable store?**

That’s where **data types** come in.

## **What Are Data Types?**

A **data type** specifies the type of data that a variable can hold.

For example:

Here:

- dailyPracticeMinutes → variable
- int → data type

This means the variable can store only integer values.

## **Java is Statically Typed**

Java requires every variable to be declared with a data type before it is used.

This is valid:

This is invalid:

Java must know the data type in advance.

# **Primitive Data Types in Java**

Java has **8 predefined primitive data types**. These are the most basic data storage types.

## **1) boolean**

Stores logical values: true or false.

Default value: false

Used in:

- Conditions
- Validation checks
- Access control logic

## **2) byte**

Range: -128 to 127

Size: 8-bit

Used when memory efficiency matters.

Example:

## **3) short**

Range: -32,768 to 32,767

Size: 16-bit

Used when integer range is small but larger than byte.

Example:

## **4) int**

Range: -2³¹ to 2³¹-1

Size: 32-bit

Most commonly used integer type.

Example:

For most placement problems, int is the default integer choice.

## **5) long**

Range: -2⁶³ to 2⁶³-1

Size: 64-bit

Used for very large numbers.

Example:

## **6) double**

64-bit floating-point type.

Used for decimal numbers.

Example:

> [!NOTE]
> **WARNING**
> ⚠ Never use double for precise currency calculations due to rounding issues.

## **7) float**

32-bit floating-point type.

Less precise than double.

Example:

> [!NOTE]
> **INFO**
> Notice the F. Without F, Java treats decimal values as double.
> 
> Default value: 0.0f

## **8) char**

Stores a single Unicode character.

Size: 16-bit

Example:

# **Primitive vs Reference Types**

The 8 types above are **primitive types**. Java also supports **reference types**, such as:

- String
- Arrays
- Classes
- Objects

## **String (Reference Type)**

Strings are not primitive. They are objects of java.lang.String.

Example:

# **Why Data Types Matter (Placement Perspective)**

In interviews, companies test:

- Proper data type selection
- Overflow handling
- Precision issues
- Memory optimization
- Type mismatch errors

Example:

If problem constraints allow values up to 10¹²,

using int will cause overflow.

You must use long.

Strong data type understanding prevents:

- Runtime errors
- Wrong answers
- Edge case failures

# **Quick Comparison Table**

# **Practical Exercise**

Try creating variables for:

- Number of contests attended
- Average problem solving time
- Whether premium member or not
- Grade in mock test

Run them inside the Quick Compiler and observe:

- Type behavior
- Literal assignment
- Output differences

Consistent hands-on practice builds real clarity.

# **What’s Next?**

Now that you understand data types, the next logical step is:

- Type Casting
- Operators
- Taking Input using Scanner
- Conditional Statements

Strong foundations here make DSA and backend development much easier.



---
*Extracted from CodeHelp (https://www.codehelp.in/articles/article-1772429161660-5shizn)*
