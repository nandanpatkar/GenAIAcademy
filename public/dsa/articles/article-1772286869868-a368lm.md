# Variables and literals 

> **Slug:** `article-1772286869868-a368lm`  
> **Published:** 2026-04-16T15:49:21.708Z  
> **Updated:** 2026-04-16T15:49:21.718Z  
> **Keywords:** variables in java, literals in java  
> **Cover Image:** ![Variables and literals ](69d1065c6068419dc789e870)

**Description:** Learn Java variables and literals, how data is stored, types of literals, syntax rules, and why they matter for coding, DSA, and interviews.

---

# **Java Variables and Literals**

In the previous tutorial, you wrote your first Java program. Now let’s understand something fundamental:

> [!NOTE]
> **INFO**
> 🤓  **How Java stores data.**

That’s where **variables** and **literals** come in.

# **Java Variables**

A **variable** is a named memory location used to store data.

Every variable in Java must have:

- A data type
- A unique name
- A value (optional at declaration)

## **Creating Variables in Java**

Let’s create a variable that stores the number of problems solved:

Here:

- int → data type
- problemsSolved → variable name
- 120 → value stored

This means we are storing the value 120 inside a memory location named problemsSolved.

## **Declaration and Assignment**

You can declare and assign separately.

Or do both together:

Both approaches are correct.

## **Java is Statically Typed**

Java requires that every variable be declared before it is used.

This is valid:

This is invalid:

Java must know the data type first.

## **Changing Variable Values**

The value stored inside a variable can change.

The memory location stays the same, but the value updates.

That’s why it is called a **variable**.

## **Important Rule: Type Cannot Change**

Once declared, a variable cannot change its data type.

This is invalid:

Java does not allow redeclaration with a different type in the same scope.

# **Naming Rules for Variables**

Follow these rules:

- Can contain letters, digits, _, $
- Cannot start with a digit
- Cannot use Java keywords
- Case-sensitive

Example:

# **Java Literals**

A **literal** is the actual fixed value assigned to a variable.

Example:

Here:

- activeUsers → variable
- 5000 → literal

Java supports multiple types of literals.

# **Integer Literals**

Integer literals represent whole numbers.

Java supports 4 formats:

### **1️⃣ Decimal (Base 10)**

### **2️⃣ Binary (Base 2)**

0b indicates binary.

### **3️⃣ Octal (Base 8)**

Leading 0 indicates octal.

### **4️⃣ Hexadecimal (Base 16)**

0x indicates hexadecimal.

> [!NOTE]
> **INFO**
> Integer literals are used with:
> 
> byte, short, int, long.

# **Floating-Point Literals**

Floating-point literals represent decimal numbers.

Example:

By default:

Decimal numbers are treated as double.

To store in float, you must use F.

## **Scientific Notation**

This means:

2.5 × 10³ = 2500

This format is useful in data analytics and performance calculations.

# **Character Literals**

Character literals represent a single character enclosed in single quotes.

You can also use escape sequences:

Escape characters represent special formatting.

# **String Literals**

A string literal is a sequence of characters inside double quotes.

Anything inside double quotes is treated as text.

# **Boolean Literals**

Boolean literals represent logical values.

They can only be:

Only true and false are valid.

# **Why Variables Matter for Placements**

In interviews, companies test:

- Variable declaration clarity
- Correct data type usage
- Memory understanding
- Type mismatch errors
- Edge case handling

Most beginner mistakes happen because of poor understanding of:

- Data types
- Literal formats
- Implicit type assumptions

Strong fundamentals here prevent errors later in DSA and system-level programming.

# **Quick Summary**



# **Practice Task**

Try creating variables for:

- Your contest rating
- Total problems solved
- Platform subscription status
- Your name

Run them inside our Quick Compiler and observe the behavior.

Consistent practice builds clarity.



---
*Extracted from CodeHelp (https://www.codehelp.in/articles/article-1772286869868-a368lm)*
