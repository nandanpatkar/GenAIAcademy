{"slug":"longest-cycle-in-a-graph","title":"Longest Cycle in a Graph","statement":"# 2360. Longest Cycle in a Graph\n\n**Difficulty:** Hard\n\n**Patterns:** Graph \u2192 DFS (Connectivity)\n\n**LeetCode:** https://leetcode.com/problems/longest-cycle-in-a-graph/\n\n**Topics:** Depth-First Search, Breadth-First Search, Graph Theory, Topological Sort, Kosaraju's Algorithm, Tarjan's SCC Algorithm\n\n---\n\n## Problem Statement\n\nYou are given a **directed** graph of `n` nodes numbered from `0` to `n - 1`, where each node has **at most one** outgoing edge.\n\nThe graph is represented with a given **0-indexed** array `edges` of size `n`, indicating that there is a directed edge from node `i` to node `edges[i]`. If there is no outgoing edge from node `i`, then `edges[i] == -1`.\n\nReturn *the length of the **longest** cycle in the graph*. If no cycle exists, return `-1`.\n\nA cycle is a path that starts and ends at the **same** node.\n\n**Example 1:**\n\n![](https://assets.leetcode.com/uploads/2022/06/08/graph4drawio-5.png)\n\n```\nInput: edges = [3,3,4,2,3]\nOutput: 3\nExplanation: The longest cycle in the graph is the cycle: 2 -> 4 -> 3 -> 2.\nThe length of this cycle is 3, so 3 is returned.\n```\n\n**Example 2:**\n\n![](https://assets.leetcode.com/uploads/2022/06/07/graph4drawio-1.png)\n\n```\nInput: edges = [2,-1,3,1]\nOutput: -1\nExplanation: There are no cycles in this graph.\n```\n\n**Constraints:**\n\n- `n == edges.length`\n\n- `2 <= n <= 10^5`\n\n- `-1 <= edges[i] < n`\n\n- `edges[i] != i`\n\n## Hints\n\n1. How many cycles can each node at most be part of?\n2. Each node can be part of at most one cycle. Start from each node and find the cycle that it is part of if there is any. Save the already visited nodes to not repeat visiting the same cycle multiple times.\n","starterCode":"class Solution:\n    def longestCycle(self, edges: List[int]) -> int:\n        pass\n","solution":"\"\"\"Longest Cycle in a Graph \u2014 functional graph, one outgoing edge per node.\n\nEach walk either reaches a visited node or forms a cycle; timestamping when a\nnode was first seen in the *current* walk gives the cycle length in O(1).\n\nTime: O(n)   Space: O(n)\n\"\"\"\nfrom typing import List\n\n\nclass Solution:\n    def longestCycle(self, edges: List[int]) -> int:\n        n = len(edges)\n        visited_at = [0] * n            # global order in which nodes were seen\n        best, clock = -1, 1\n        for start in range(n):\n            if visited_at[start]:\n                continue\n            start_clock = clock\n            node = start\n            while node != -1 and not visited_at[node]:\n                visited_at[node] = clock\n                clock += 1\n                node = edges[node]\n            if node != -1 and visited_at[node] >= start_clock:\n                best = max(best, clock - visited_at[node])\n        return best\n","approach":"Each walk either reaches a visited node or forms a cycle; timestamping when a node was first seen in the *current* walk gives the cycle length in O(1).","visibleTests":[{"id":"example-1","input":{"edges":[3,3,4,2,3]},"expected":3},{"id":"example-2","input":{"edges":[2,-1,3,1]},"expected":-1}],"hiddenTestCount":0,"judgeAvailable":true}