{"slug":"network-delay-time","title":"Network Delay Time","statement":"# 743. Network Delay Time\n\n**Difficulty:** Medium\n\n**Patterns:** Graph \u2192 Dijkstra (Weighted)\n\n**LeetCode:** https://leetcode.com/problems/network-delay-time/\n\n**Topics:** Depth-First Search, Breadth-First Search, Graph Theory, Heap (Priority Queue), Shortest Path, Dijkstra's Algorithm\n\n---\n\n## Problem Statement\n\nYou are given a network of `n` nodes, labeled from `1` to `n`. You are also given `times`, a list of travel times as directed edges `times[i] = (u_i, v_i, w_i)`, where `u_i` is the source node, `v_i` is the target node, and `w_i` is the time it takes for a signal to travel from source to target.\n\nWe will send a signal from a given node `k`. Return *the **minimum** time it takes for all the* `n` *nodes to receive the signal*. If it is impossible for all the `n` nodes to receive the signal, return `-1`.\n\n**Example 1:**\n\n![](https://assets.leetcode.com/uploads/2019/05/23/931_example_1.png)\n\n```\nInput: times = [[2,1,1],[2,3,1],[3,4,1]], n = 4, k = 2\nOutput: 2\n```\n\n**Example 2:**\n\n```\nInput: times = [[1,2,1]], n = 2, k = 1\nOutput: 1\n```\n\n**Example 3:**\n\n```\nInput: times = [[1,2,1]], n = 2, k = 2\nOutput: -1\n```\n\n**Constraints:**\n\n- `1 <= k <= n <= 100`\n\n- `1 <= times.length <= 6000`\n\n- `times[i].length == 3`\n\n- `1 <= u_i, v_i <= n`\n\n- `u_i != v_i`\n\n- `0 <= w_i <= 100`\n\n- All the pairs `(u_i, v_i)` are **unique**. (i.e., no multiple edges.)\n\n## Hints\n\n1. We visit each node at some time, and if that time is better than the fastest time we've reached this node, we travel along outgoing edges in sorted order.  Alternatively, we could use Dijkstra's algorithm.\n","starterCode":"class Solution:\n    def networkDelayTime(self, times: List[List[int]], n: int, k: int) -> int:\n        pass\n","solution":"\"\"\"Network Delay Time \u2014 Dijkstra from the source node.\n\nThe answer is the largest shortest-path distance; unreachable nodes make it -1.\n\nTime: O(E log V)   Space: O(V + E)\n\"\"\"\nimport heapq\nfrom collections import defaultdict\nfrom typing import List\n\n\nclass Solution:\n    def networkDelayTime(self, times: List[List[int]], n: int, k: int) -> int:\n        graph = defaultdict(list)\n        for u, v, w in times:\n            graph[u].append((v, w))\n\n        dist = {}\n        heap = [(0, k)]\n        while heap:\n            time, node = heapq.heappop(heap)\n            if node in dist:\n                continue\n            dist[node] = time\n            for nxt, weight in graph[node]:\n                if nxt not in dist:\n                    heapq.heappush(heap, (time + weight, nxt))\n        return max(dist.values()) if len(dist) == n else -1\n","approach":"The answer is the largest shortest-path distance; unreachable nodes make it -1.","visibleTests":[{"id":"example-1","input":{"times":[[2,1,1],[2,3,1],[3,4,1]],"n":4,"k":2},"expected":2},{"id":"example-2","input":{"times":[[1,2,1]],"n":2,"k":1},"expected":1},{"id":"example-3","input":{"times":[[1,2,1]],"n":2,"k":2},"expected":-1}],"hiddenTestCount":0,"judgeAvailable":true}