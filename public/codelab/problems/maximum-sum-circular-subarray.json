{"slug":"maximum-sum-circular-subarray","title":"Maximum Sum Circular Subarray","statement":"# 918. Maximum Sum Circular Subarray\n\n**Difficulty:** Medium\n\n**Patterns:** Array \u2192 Kadane\u2019s Algorithm\n\n**LeetCode:** https://leetcode.com/problems/maximum-sum-circular-subarray/\n\n**Topics:** Array, Divide and Conquer, Dynamic Programming, Queue, Monotonic Queue\n\n---\n\n## Problem Statement\n\nGiven a **circular integer array** `nums` of length `n`, return *the maximum possible sum of a non-empty **subarray** of *`nums`.\n\nA **circular array** means the end of the array connects to the beginning of the array. Formally, the next element of `nums[i]` is `nums[(i + 1) % n]` and the previous element of `nums[i]` is `nums[(i - 1 + n) % n]`.\n\nA **subarray** may only include each element of the fixed buffer `nums` at most once. Formally, for a subarray `nums[i], nums[i + 1], ..., nums[j]`, there does not exist `i <= k1`, `k2 <= j` with `k1 % n == k2 % n`.\n\n**Example 1:**\n\n```\nInput: nums = [1,-2,3,-2]\nOutput: 3\nExplanation: Subarray [3] has maximum sum 3.\n```\n\n**Example 2:**\n\n```\nInput: nums = [5,-3,5]\nOutput: 10\nExplanation: Subarray [5,5] has maximum sum 5 + 5 = 10.\n```\n\n**Example 3:**\n\n```\nInput: nums = [-3,-2,-3]\nOutput: -2\nExplanation: Subarray [-2] has maximum sum -2.\n```\n\n**Constraints:**\n\n- `n == nums.length`\n\n- `1 <= n <= 3 * 10^4`\n\n- `-3 * 10^4 <= nums[i] <= 3 * 10^4`\n\n## Hints\n\n1. For those of you who are familiar with the **Kadane's algorithm**, think in terms of that. For the newbies, Kadane's algorithm is used to finding the maximum sum subarray from a given array. This problem is a twist on that idea and it is advisable to read up on that algorithm first before starting this problem. Unless you already have a great algorithm brewing up in your mind in which case, go right ahead!\n2. What is an alternate way of representing a circular array so that it appears to be a straight array?\nEssentially, there are two cases of this problem that we need to take care of. Let's look at the figure below to understand those two cases:\n\n\n\n\n![figure](https://assets.leetcode.com/uploads/2019/10/20/circular_subarray_hint_1.png)\n3. The first case can be handled by the good old Kadane's algorithm. However, is there a smarter way of going about handling the second case as well?\n","starterCode":"class Solution:\n    def maxSubarraySumCircular(self, nums: List[int]) -> int:\n        pass\n","solution":"\"\"\"Maximum Sum Circular Subarray \u2014 Kadane twice.\n\nA wrapping subarray is the total minus a non-wrapping *minimum* subarray. The\nall-negative case is the one exception: the \"wrap\" candidate would be empty.\n\nTime: O(n)   Space: O(1)\n\"\"\"\nfrom typing import List\n\n\nclass Solution:\n    def maxSubarraySumCircular(self, nums: List[int]) -> int:\n        total = 0\n        cur_max = best_max = nums[0]\n        cur_min = best_min = nums[0]\n        for i, value in enumerate(nums):\n            total += value\n            if i == 0:\n                continue\n            cur_max = max(value, cur_max + value)\n            best_max = max(best_max, cur_max)\n            cur_min = min(value, cur_min + value)\n            best_min = min(best_min, cur_min)\n        if best_max < 0:\n            return best_max            # every element is negative\n        return max(best_max, total - best_min)\n","approach":"A wrapping subarray is the total minus a non-wrapping *minimum* subarray. The all-negative case is the one exception: the \"wrap\" candidate would be empty.","visibleTests":[{"id":"example-1","input":{"nums":[1,-2,3,-2]},"expected":3},{"id":"example-2","input":{"nums":[5,-3,5]},"expected":10},{"id":"example-3","input":{"nums":[-3,-2,-3]},"expected":-2}],"hiddenTestCount":0,"judgeAvailable":true}