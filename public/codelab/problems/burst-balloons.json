{"slug":"burst-balloons","title":"Burst Balloons","statement":"# 312. Burst Balloons\n\n**Difficulty:** Hard\n\n**Patterns:** Dynamic Programming \u2192 DP on Intervals\n\n**LeetCode:** https://leetcode.com/problems/burst-balloons/\n\n**Topics:** Array, Dynamic Programming\n\n---\n\n## Problem Statement\n\nYou are given `n` balloons, indexed from `0` to `n - 1`. Each balloon is painted with a number on it represented by an array `nums`. You are asked to burst all the balloons.\n\nIf you burst the `i^th` balloon, you will get `nums[i - 1] * nums[i] * nums[i + 1]` coins. If `i - 1` or `i + 1` goes out of bounds of the array, then treat it as if there is a balloon with a `1` painted on it.\n\nReturn *the maximum coins you can collect by bursting the balloons wisely*.\n\n**Example 1:**\n\n```\nInput: nums = [3,1,5,8]\nOutput: 167\nExplanation:\nnums = [3,1,5,8] --> [3,5,8] --> [3,8] --> [8] --> []\ncoins =  3*1*5    +   3*5*8   +  1*3*8  + 1*8*1 = 167\n```\n\n**Example 2:**\n\n```\nInput: nums = [1,5]\nOutput: 10\n```\n\n**Constraints:**\n\n- `n == nums.length`\n\n- `1 <= n <= 300`\n\n- `0 <= nums[i] <= 100`\n","starterCode":"class Solution:\n    def maxCoins(self, nums: List[int]) -> int:\n        pass\n","solution":"\"\"\"Burst Balloons \u2014 interval DP on the *last* balloon to pop.\n\nChoosing which balloon bursts first couples the subproblems; choosing the last\none in a range leaves two independent ranges with known boundaries.\n\nTime: O(n^3)   Space: O(n^2)\n\"\"\"\nfrom typing import List\n\n\nclass Solution:\n    def maxCoins(self, nums: List[int]) -> int:\n        values = [1] + nums + [1]\n        n = len(values)\n        dp = [[0] * n for _ in range(n)]\n        for length in range(2, n):\n            for left in range(n - length):\n                right = left + length\n                for last in range(left + 1, right):\n                    dp[left][right] = max(\n                        dp[left][right],\n                        dp[left][last] + dp[last][right]\n                        + values[left] * values[last] * values[right])\n        return dp[0][n - 1]\n","approach":"Choosing which balloon bursts first couples the subproblems; choosing the last one in a range leaves two independent ranges with known boundaries.","visibleTests":[{"id":"example-1","input":{"nums":[3,1,5,8]},"expected":167},{"id":"example-2","input":{"nums":[1,5]},"expected":10}],"hiddenTestCount":0,"judgeAvailable":true}